import { StyleSheet } from "react-native";

export const navStyles = StyleSheet.create({
    primaryBG: {
      backgroundColor: "#694fad"
    },
    primaryColor: {
      color: "#f0edf6"
    },
    secondaryColor: {
      color: "#3e2465"
    }
  });